<?xml version='1.0' encoding='UTF-8'?>
<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" version="2.0"><channel><title>aoterniu 个人博客</title><link>https://aoterniu.github.io</link><description>aoterniu 个人博客</description><copyright>aoterniu 个人博客</copyright><docs>http://www.rssboard.org/rss-specification</docs><generator>python-feedgen</generator><image><url>https://avatars.githubusercontent.com/u/207451910?v=4</url><title>avatar</title><link>https://aoterniu.github.io</link></image><lastBuildDate>Wed, 16 Apr 2025 10:37:24 +0000</lastBuildDate><managingEditor>aoterniu 个人博客</managingEditor><ttl>60</ttl><webMaster>aoterniu 个人博客</webMaster><item><title>飞牛OS开启IOMMU硬件直通</title><link>https://aoterniu.github.io/post/fei-niu-OS-kai-qi-IOMMU-ying-jian-zhi-tong.html</link><description># 飞牛OS开启IOMMU硬件直通完整指南

## 🔧 前置准备
### 开启SSH远程访问
1. **打开MobaXterm**  
   - 点击 `Session` → `SSH`
2. **配置连接参数**  
   ```bash
   # 连接参数示例
   Host     : 192.168.1.100  # 飞牛OS管理IP
   Port     : 22
   Username : root           # 默认管理员账户
   ```

---

## 🖥️ 核心配置流程
### 修改GRUB引导配置
```bash
sudo nano /etc/default/grub
```

#### 处理器类型配置
&lt;table&gt;
&lt;tr&gt;
&lt;th&gt;Intel平台&lt;/th&gt;
&lt;th&gt;AMD平台&lt;/th&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;

```bash
GRUB_CMDLINE_LINUX_DEFAULT='quiet i915.force_probe=7d55 intel_iommu=on iommu=pt'
```
&lt;/td&gt;
&lt;td&gt;

```bash
GRUB_CMDLINE_LINUX_DEFAULT='quiet i915.force_probe=7d55 amd_iommu=on iommu=pt'
```
&lt;/td&gt;
&lt;/tr&gt;
&lt;/table&gt;

#### 操作说明
1. 使用 `Ctrl+W` 搜索原配置行
2. 按处理器类型修改参数
3. 保存退出：`Ctrl+S` → `Ctrl+X`

---

### 应用配置更新
```bash
# 更新GRUB配置
sudo update-grub

# 重建initramfs
sudo update-initramfs -u -k all

# 重启系统
sudo reboot
```

#### 预期成功输出
```bash
update-initramfs: Generating /boot/initrd.img-5.15.0-101-generic
Warning: No support for locale: zh_CN.utf8
```

---

## ✅ 验证IOMMU状态
### 检查内核日志
```bash
dmesg | grep -i iommu
```

#### 成功标志
```bash
[    0.000000] DMAR: IOMMU enabled
[    0.598712] iommu: Default domain type: Translated 
```

---

## ⚠️ 注意事项
&lt;details&gt;
&lt;summary&gt;点击查看风险提示&lt;/summary&gt;

1. **硬件兼容性**  
   ```bash
   # 检查CPU虚拟化支持
   grep -E 'vmx|svm' /proc/cpuinfo
   ```
   - Intel需输出 `vmx`，AMD需输出 `svm`

2. **回滚方案**  
   ```bash
   # 恢复原始配置
   sudo nano /etc/default/grub → 删除iommu参数
   sudo update-grub
   sudo reboot
   ```

3. **日志监控**  
   ```bash
   journalctl -b -0 | grep -i dmar
   ```
&lt;/details&gt;

---

## 🖼️ 效果预览
![配置流程图](https://via.placeholder.com/800x400.png/000/fff?text=IOMMU+Configuration+Flow)
。</description><guid isPermaLink="true">https://aoterniu.github.io/post/fei-niu-OS-kai-qi-IOMMU-ying-jian-zhi-tong.html</guid><pubDate>Wed, 16 Apr 2025 10:36:58 +0000</pubDate></item><item><title>飞牛系统下 php环境安装失败</title><link>https://aoterniu.github.io/post/fei-niu-xi-tong-xia-%20php-huan-jing-an-zhuang-shi-bai.html</link><description># 一条命令解决飞牛OS下1Panel安装PHP环境失败的问题

```bash
sudo apt install docker-compose --allow-change-held-packages -y
```

---

## 命令解析
### 核心指令模块
```bash
sudo apt install docker-compose
```
- **作用**  
  📦 安装 Docker 容器编排工具，用于管理多容器 PHP 环境
- **必要性**  
  🔧 1Panel 依赖 docker-compose 实现应用容器化部署

---

### 参数详解
#### 强制覆盖参数
```bash
--allow-change-held-packages
```
| 特性        | 说明                                                                 |
|-------------|--------------------------------------------------------------------|
| 适用场景    | 当出现 `E: 无法修正错误，因为您要求某些软件包保持现状` 错误时使用              |
| 风险控制    | 建议先执行 `apt-mark showhold` 查看被锁定的包                        |
| 飞牛OS特性  | 系统默认锁定部分核心包版本以保持稳定性                                  |

#### 自动化参数
```bash
-y
```
```bash
# 等效写法（参数位置调整）
sudo apt install -y docker-compose
```
- **优势**  
  ✅ 跳过交互确认流程，适合自动化部署
- **注意事项**  
  ⚠️ 使用前需确保已校验安装包安全性

---

## 完整解决方案
```bash
# 推荐三步式安装流程
sudo apt update --fix-missing
sudo apt install docker-compose --allow-change-held-packages -y --reinstall
sudo systemctl restart docker
```

---

## 效果验证
```bash
# 版本检查（成功示例）
docker-compose --version
# 预期输出
docker-compose version 1.29.2, build unknown

# 服务状态检查
systemctl status docker | grep Active
# 预期输出
Active: active (running)
```

---

## ⚠️ 注意事项
| 风险类型       | 应对方案                                                                 |
|----------------|------------------------------------------------------------------------|
| 依赖冲突       | 先执行 `sudo apt --fix-broken install`                                |
| GPG密钥错误    | 使用 `sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys [ID]` |
| 系统版本兼容   | 仅适用于飞牛OS 22.04 LTS+ 版本                                         |

---

## 常见问题 (FAQ)
&lt;details&gt;
&lt;summary&gt;点击查看问题解答&lt;/summary&gt;

**Q1: 为什么必须使用 `--allow-change-held-packages` 参数？**  
👉 飞牛OS 通过 `apt-mark hold` 锁定关键软件包版本，此参数强制解除保护

**Q2: 如何安全回滚操作？**  
```bash
# 卸载并恢复锁定
sudo apt purge docker-compose -y
sudo apt-mark hold docker-compose
```

**Q3: 安装后 docker 服务无法启动怎么办？**  
```bash
# 查看日志定位问题
journalctl -u docker.service --since '5 minutes ago'
```
&lt;/details&gt;

---

**效果预览**：  
![GitHub Markdown 链接](一条命令解决飞牛OS下1Panel安装PHP环境失败的问题
https://club.fnnas.com/forum.php?mod=viewthread&amp;tid=6441
)。</description><guid isPermaLink="true">https://aoterniu.github.io/post/fei-niu-xi-tong-xia-%20php-huan-jing-an-zhuang-shi-bai.html</guid><pubDate>Wed, 16 Apr 2025 10:11:56 +0000</pubDate></item><item><title>Windows 网络高级配置</title><link>https://aoterniu.github.io/post/Windows%20-wang-luo-gao-ji-pei-zhi.html</link><description># Windows 网络高级配置指南

## 一、调整 IPv4/IPv6 优先级
### 修改注册表使 IPv4 优先

1. **打开注册表编辑器**：
   ```reg
   Win + R → 输入 regedit → Enter
   ```

2. **定位到路径**：
   ```reg
   HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\Tcpip6\Parameters
   ```

3. **新建/修改 DWORD 值**：
   ```reg
   右键新建 → DWORD (32位) → 命名为 DisabledComponents
   ```

4. **设置值数据**：
   ```reg
   0x20：禁用 IPv6 部分功能（IPv4优先）
   0xFF：完全禁用 IPv6
   ```
   - 推荐值：`0x20`（保持 IPv6 部分功能）
   - 危险值：`0xFF`（完全禁用 IPv6）

5. **重启系统生效**
   ```powershell
   shutdown /r /t 0
   ```

---

## 格式说明
### 优化要点：
1. **代码块规范**：
   ```markdown
   ```reg  # 注册表操作专用代码块
   ```powershell  # PowerShell 命令块
   ```

2. **层级结构**：
   - 使用 `#` → `##` → `###` 三级标题
   - 操作步骤使用有序列表（数字编号）

3. **高亮强调**：
   - 关键路径用 **加粗**
   - 危险操作值用红色标注（GitHub 需CSS支持，此处用文字说明）

### GitHub 预览效果：
![示例](https://via.placeholder.com/800x400.png/000000/FFFFFF?text=Markdown+Preview)


## ⚠️ 注意事项
- 修改前备份注册表（`文件 → 导出`）
- 需要管理员权限
- Windows 11/10 不同版本路径可能微调

# 快速重启命令
shutdown /r /t 0

| 值数据 | 效果                  | 推荐指数 |
|--------|-----------------------|----------|
| 0x20   | IPv4优先（兼容模式）  | ★★★★★   |
| 0xFF   | 完全禁用IPv6          | ★★☆☆☆   |


# Windows 网络高级配置指南

---

## 二、锁定 ARP 静态表
### 防止 ARP 缓存被修改

1. **定位注册表路径**：
   ```reg
   HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters
   ```

2. **修改 ARP 重试次数**：
   ```reg
   ; 注册表项配置
   项名称：ArpRetryCount
   类型：DWORD (32位)
   值数据：0
   ```
   - **效果**：ARP 缓存表将拒绝自动更新
   - **风险提示**：可能导致合法设备无法动态接入网络

---

## 三、手动配置 DNS-over-HTTPS (DoH)
### 自定义安全 DNS 服务器

1. **创建 DNS 策略键**：
   ```reg
   HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\Dns
   ```
   &gt; 如路径不存在：右键 `Windows` 文件夹 → 新建项 → 命名 `Dns`

2. **添加 DoH 服务器地址**：
   ```reg
   ; 新建字符串值
   右键空白处 → 新建 → 字符串值 → 命名 DoHServers
   ```

3. **输入服务器地址**：
   ```txt
   # Cloudflare (推荐)
   https://dns.cloudflare.com/dns-query

   # Google
   https://dns.google/dns-query

   # 自定义服务器
   https://your-doh-server/dns-query
   ```

4. **生效配置**：
   ```powershell
   # 强制刷新 DNS
   ipconfig /flushdns
   # 重启系统
   shutdown /r /t 5
   ```

---

### ⚠️ 通用注意事项
| 类别        | 重要提醒                                  |
|-------------|-----------------------------------------|
| 权限要求    | 所有操作需使用 **管理员账户**            |
| 系统兼容性  | 适用于 Windows 10 20H1+ / Windows 11    |
| 风险控制    | 建议先通过 `文件 → 导出` 备份注册表      |
| 回滚操作    | 删除添加的注册表项即可恢复默认设置       |

### 推荐参数组合
```reg
; 最佳安全配置方案
DisabledComponents = 0x20  (IPv4优先)
ArpRetryCount      = 0     (锁定ARP)
DoHServers         = https://dns.cloudflare.com/dns-query
```

---

**效果预览**：  
![GitHub Markdown 渲染效果](https://via.placeholder.com/800x400.png/000/fff?text=Optimized+for+GitHub+Issues)

。</description><guid isPermaLink="true">https://aoterniu.github.io/post/Windows%20-wang-luo-gao-ji-pei-zhi.html</guid><pubDate>Mon, 14 Apr 2025 10:20:00 +0000</pubDate></item></channel></rss>